# Initialise the universal variables
# Done this way so I don't accidentally commit any more
# Github access tokens. If there's a better way, I'd love to see it
source $HOME/dotfiles/fish/variables.fish