#!/usr/bin/env bash

case $( uname -a | tr '[:upper:]' '[:lower:]') in
  msys*|cygwin*|mingw*|*microsoft*|nt|win*)
    # or possible 'bash on windows'
    echo 'windows'
    ;;
  darwin*)
    echo 'darwin'
    ;;
  linux*)
    echo 'linux'
    ;;
  *)
    echo 'unknown'
    ;;
esac
