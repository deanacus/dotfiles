# Basic BASH prompt. Used to have the git branch and a status indicator, but with
# the upgrade to Mavericks it stopped functioning correctly. So I pulled it out.
# Maybe one day it'll go back in.

CYAN=$(tput setaf 6)
MAGENTA=$(tput setaf 5)
GREEN=$(tput setaf 2)
ORANGE=$(tput setaf 172)
WHITE=$(tput setaf 7)
RED=$(tput setaf 1)

function gitstatus {
  branch=
  if [[ -z $(git branch 2> /dev/null) ]]; then
    return
  else
    [[ $(git status 2> /dev/null | tail -n1) != "nothing to commit, working directory clean" ]] && echo "*"
  fi
}

export PS1="\n\[$CYAN\]\u \[$WHITE\]at \[$MAGENTA\]\h \[$WHITE\]in \[$GREEN\]\w\[$RED\]$(gitstatus)\[$WHITE\]\n> "